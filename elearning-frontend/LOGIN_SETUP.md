# ระบบ Login สำหรับ Nuxt elearning

ฉันได้สร้างระบบ login ที่สมบูรณ์สำหรับแอปพลิเคชัน elearning Nuxt ของคุณแล้ว

## 📁 ไฟล์ที่สร้างขึ้น

### 1. **หน้า Login** (`pages/login.vue`)
   - หน้าเพดานที่สมบูรณ์พร้อมแบบฟอร์ม
   - ดีไซน์ที่เป็นมิตรกับผู้ใช้โดยใช้ Tailwind CSS
   - ตัวตรวจสอบการป้อนข้อมูล
   - ลิงค์ไปยังการลืมรหัสผ่านและลงทะเบียน

### 2. **คอมโพเนนต์ LoginForm** (`components/LoginForm.vue`)
   - คอมโพเนนต์ที่สามารถใช้ซ้ำได้
   - เดียวกันกับฟีเจอร์ของหน้า login
   - สามารถใช้ในที่อื่น ๆ ในแอปพลิเคชันของคุณ

### 3. **Authentication Composable** (`composables/useAuth.ts`)
   - จัดการสถานะการเข้าสู่ระบบ
   - จัดการโทเค็น
   - ฟังก์ชันเข้าสู่ระบบและออกจากระบบ
   - ตรวจสอบการรับรองความถูกต้อง

### 4. **Auth Middleware** (`middleware/auth.ts`)
   - ป้องกันเส้นทาง
   - เปลี่ยนเส้นทางผู้ใช้ที่ไม่ได้รับการตรวจสอบไปยังหน้า login
   - เปลี่ยนเส้นทางผู้ใช้ที่ได้รับการตรวจสอบไปยังแดชบอร์ด

### 5. **Dashboard Page** (`pages/dashboard.vue`)
   - ตัวอย่างหน้าที่มีการป้องกัน
   - แสดงวิธีการใช้ composable
   - ปุ่มออกจากระบบ

## 🚀 วิธีการใช้

### ขั้นตอนที่ 1: สร้าง API Endpoint สำหรับ Login
ในแบ็กเอนด์ Laravel (`elearning-backend/routes/api.php`):

```php
Route::post('/login', [ApiController::class, 'login']);
Route::post('/logout', [ApiController::class, 'logout'])->middleware('auth:sanctum');
```

### ขั้นตอนที่ 2: ใช้หน้า Login
URL: `http://localhost:3000/login`

### ขั้นตอนที่ 3: ใช้ Composable ใน Component อื่น

```vue
<script setup>
const { login, logout, isAuthenticated, user } = useAuth()

const handleLogin = async () => {
  try {
    await login('user@example.com', 'password123', true)
  } catch (error) {
    console.error('Login failed:', error)
  }
}

const handleLogout = async () => {
  await logout()
}
</script>
```

### ขั้นตอนที่ 4: ป้องกันเส้นทาง
ในหน้าใด ๆ ที่ต้องการป้องกัน:

```vue
<script setup>
definePageMeta({
  middleware: 'auth'
})
</script>
```

## 🔌 การเชื่อมต่อกับแบ็กเอนด์

### ตัวอย่าง API Response สำหรับ Login

```json
{
  "token": "your_bearer_token_here",
  "user": {
    "id": 1,
    "email": "user@example.com",
    "name": "John Doe"
  }
}
```

### การตั้งค่า Authentication Header
Composable จะจัดเก็บโทเค็นโดยอัตโนมัติใน localStorage/sessionStorage และสามารถใช้เพื่อตั้งค่า Authorization header:

```typescript
const auth = useAuth()
const headers = {
  'Authorization': `Bearer ${auth.getToken()}`
}
```

## 🎨 ปรับแต่งสไตล์

หน้า login ใช้ Tailwind CSS ที่กำลังตั้งค่าในโปรเจกต์ของคุณแล้ว

คุณสามารถปรับแต่ง:
- สีแบรนด์ (ปัจจุบัน: Indigo)
- ข้อความ (ปัจจุบัน: ภาษาไทย)
- การจัดวาง
- ฟอร์ม fields

## 📋 Checklist

- ✅ สร้างหน้า login
- ✅ สร้างคอมโพเนนต์ form
- ✅ สร้าง auth composable
- ✅ สร้าง auth middleware
- ✅ สร้างตัวอย่างหน้า dashboard
- ⏳ สร้าง API endpoint ในแบ็กเอนด์ (คุณต้องทำ)
- ⏳ ปรับแต่งให้เหมาะสมกับบรรนดาธิการของคุณ (คุณต้องทำ)

## 📝 หมายเหตุสำคัญ

1. **โทเค็นจัดเก็บ**: โทเค็นจัดเก็บใน localStorage (ถ้า remember me) หรือ sessionStorage
2. **API URL**: ปัจจุบันชี้ไปที่ `/api/login` - อัพเดตตามความจำเป็น
3. **ตัวตรวจสอบ**: เพิ่มตัวตรวจสอบเพิ่มเติมตามต้องการ
4. **ข้อผิดพลาด**: จัดการข้อผิดพลาด API ตามความต้องการของคุณ

## 🔄 ขั้นตอนต่อไป

1. สร้าง API endpoint login ในแบ็กเอนด์
2. ทดสอบการเชื่อมต่อที่สมบูรณ์
3. สร้างหน้า "ลืมรหัสผ่าน" และ "ลงทะเบียน"
4. เพิ่มการป้องกันสองปัจจัย (ถ้าต้องการ)
5. เพิ่ม refresh token logic

ถ้ามีคำถาม เพิ่มเติม สามารถแจ้งให้ฉันทราบได้!
